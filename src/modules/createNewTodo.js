//, dueDate, priority
class Todo {
  constructor(title) {
    this.title = title;
    // this.dueDate = dueDate;
    // this.priority = priority;
  }

  getTitle() {
    return this.title;
  }
}

export default Todo;
